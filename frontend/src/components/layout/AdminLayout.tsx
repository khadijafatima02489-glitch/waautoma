import type { ReactNode } from "react";
import { NavLink } from "react-router-dom";
import { Building2, CreditCard, LayoutDashboard, LogOut, Settings, ShieldCheck } from "lucide-react";
import { useAuth } from "@/context/AuthContext";
import ThemeToggle from "@/components/ThemeToggle";
const links=[["/admin","Dashboard",LayoutDashboard],["/admin/restaurants","Restaurants",Building2],["/admin/subscriptions","Subscriptions",CreditCard],["/admin/settings","Settings",Settings]] as const;
export default function AdminLayout({children}:{children:ReactNode}) { const {logout}=useAuth(); return <div data-testid="admin-shell" className="flex min-h-screen bg-background text-foreground"><aside className="hidden w-64 flex-col border-r bg-card md:flex"><div className="flex items-center gap-3 border-b p-6"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#D94833] text-white"><ShieldCheck size={20}/></span><div><p className="font-heading font-bold">Restaurant AI</p><p className="text-xs text-muted-foreground">Super Admin</p></div></div><nav className="flex-1 space-y-1 p-3">{links.map(([to,label,Icon])=><NavLink end={to==="/admin"} key={to} to={to} data-testid={`admin-nav-${label.toLowerCase()}`} className={({isActive})=>`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium ${isActive?"bg-[#2C614F] text-white":"text-muted-foreground hover:bg-muted hover:text-foreground"}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="flex items-center justify-between border-t p-4"><ThemeToggle/><button data-testid="admin-logout-button" onClick={logout} className="flex items-center gap-2 text-sm text-muted-foreground"><LogOut size={16}/> Logout</button></div></aside><main className="min-w-0 flex-1 p-5 md:p-9">{children}</main></div>; }